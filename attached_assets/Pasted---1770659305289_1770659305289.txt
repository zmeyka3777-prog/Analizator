ПОЛНЫЙ СИСТЕМНЫЙ АУДИТ. Проект постоянно ломается — одно чинишь, другое отваливается. Нужна полная проверка.
ЧАСТЬ 1: ЗАГРУЗКА ФАЙЛОВ (критично — ломалась 5 раз за сегодня)
Пройди весь путь загрузки файла от клика "Загрузить" до появления в Истории загрузок. Для КАЖДОГО шага напиши: работает / сломано / потенциальная проблема.

Выбор файла на фронтенде → определение размера → решение chunked/single upload
POST /api/files/upload-init → создание сессии → ответ клиенту
POST /api/files/upload-chunk (×N) → busboy парсинг → сохранение чанка на диск

Проверь race condition: гарантирован ли порядок field/file в busboy?
Проверь: если userId не совпадает или fileId не пришёл — отправляется ли ответ клиенту?
Проверь: есть ли таймаут на busboy? Что если чанк завис?


POST /api/files/upload-complete → сборка чанков → парсинг CSV

Проверь: что если сборка упала? Удаляются ли чанки? Отправляется ли ответ?


CSV парсинг → маппинг колонок → extractDayFromDate → нормализация регионов/препаратов

Проверь: что если в CSV нет колонки даты? Упадёт ли парсинг day?


COPY в raw_sales_rows → включая колонку day

Проверь: колонка day существует на Timeweb? (была проблема сегодня)


Агрегация → buildCompactRowsFromDB → yearly_sales_data
Запись compact rows чанками → writeCompactRows

Проверь: что если один чанк упал? Остальные записались?


Запись в upload_history

Проверь: запись создаётся ПОСЛЕ всех шагов? Если шаг 7 или 8 упал — запись не создаётся и файл "пропадает"?


Очистка raw_sales_rows → cleanupRawRows


Проверь: что если очистка упала? Данные дублируются?


Ответ клиенту → обновление UI

Для каждого шага напиши: что произойдёт если он упадёт? Получит ли клиент ошибку или зависнет без ответа?
ЧАСТЬ 2: УДАЛЕНИЕ ФАЙЛОВ

DELETE /api/upload-history/:id → удаление из upload_history
Проверка: остались ли файлы за этот год? → удаление yearly_sales_data
Проверка: остались ли файлы вообще? → удаление year=9999
Проверь: все ли compactRows чанки (compactRows_0, compactRows_1...) удаляются?
DELETE /api/upload-history/all → полная очистка
Проверь: VACUUM выполняется после удаления?

ЧАСТЬ 3: ОТОБРАЖЕНИЕ ДАННЫХ
Для каждого endpoint проверь: правильно ли применяются фильтры?

/api/tab/dashboard — фильтр года, региона, препарата, типа выбытия
/api/tab/wm-dashboard — wmYear + wmMonth (включая пропорциональную фильтрацию через monthlySales)
/api/tab/wm-products — wmYear + wmMonth
/api/tab/wm-districts — wmYear + wmMonth
/api/tab/territories, contragents, problems, compare, abc, seasonal, forecast, peremployee — year filter
Понедельный анализ — клиентская фильтрация weeklyAnalysisYear + weeklyAnalysisMonth

Проверь: при "Все года" + конкретный месяц — правильно ли работает fallback через monthlySales?
Проверь: year=9999 (compactRows) нигде не попадает в суммы продаж?
ЧАСТЬ 4: ПЕРЕСЧЁТ В РУБЛИ

calcRublesRatio — корректно ли считает коэффициент?
Что если drugSales пустой? ratio = NaN? Infinity?
Что если drugPrices не загрузились? Все значения = 0?
fmtValue — правильно ли форматирует большие числа?
На КАЖДОЙ вкладке: при переключении "₽" → числа реально меняются?

ЧАСТЬ 5: СТАБИЛЬНОСТЬ

Circuit breaker — правильно ли сбрасывается после восстановления БД?
Pool — текущие настройки (max, timeout, keepAlive)?
Vite proxy — есть ли таймаут на проксирование к API? Может ли Vite обрывать долгие запросы?
Что если клиент закрыл вкладку во время загрузки файла? Сервер продолжает обработку или зависает?
Что если одновременно два пользователя загружают файлы?

РЕЗУЛЬТАТ: таблица всех найденных проблем с приоритетами:
| # | Проблема | Критичность | Где | Исправлено? |
Не исправляй ничего — только аудит. Жду таблицу проблем.