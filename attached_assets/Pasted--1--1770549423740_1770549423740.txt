Исправляем все найденные проблемы. Делай по порядку приоритета:
1. КРИТИЧНО — "Исключить из анализа" не работает:
Сейчас toggleUploadActive только меняет флаг, но данные неактивного файла продолжают участвовать в аналитике. Исправь: после переключения флага — пересчитай yearly_sales_data из compactRows ТОЛЬКО активных файлов. Используй тот же подход что и при удалении: отфильтруй compact rows по uploadId неактивных загрузок, потом reaggregateFromCompactRows() из оставшихся.
2. КРИТИЧНО — Защита от дубликатов:
При загрузке файла проверяй: есть ли в upload_history запись с таким же именем файла И размером для этого пользователя. Если да — не загружать автоматически, а вернуть ошибку с сообщением: "Файл с таким названием и размером уже загружен. Удалите предыдущую версию в Истории загрузок, если хотите загрузить заново."
3. КРИТИЧНО — Нормализация регионов:
Создай единую функцию normalizeRegionName() и используй её ВЕЗДЕ:

При загрузке файлов продаж (fileProcessor.ts) — нормализовать регион перед сохранением в raw_sales_rows
При сопоставлении с population_data (tabDataRoutes.ts percapita)
При сопоставлении с employees_data территориями менеджеров

Правила нормализации:

"Респ." → "Республика"
"обл." → "область"
"кр." → "край"
"г." → "" (убрать для "г. Москва" → "Москва")
"АО" → "автономный округ" или наоборот — выбери одно направление
Убрать лишние пробелы, привести к нижнему регистру для сравнения
Обработать слипшиеся слова ("Астраханскаяобласть" → "Астраханская область")

Функция normalizeRegionName() уже есть в loadPopulation.ts — вынеси её в отдельный utils файл и используй во всех местах.
4. СРЕДНЕ — Нормализация препаратов:
Здесь нормализация сложнее, потому что названия сильно отличаются. Но хотя бы: убрать лишние пробелы, привести регистр. Полную нормализацию препаратов оставим на потом — пока файлы загружаются по препаратам, названия внутри одного файла одинаковые.
5. После всех исправлений:

Перезапусти сервер
Покажи тест: toggle "Исключить из анализа" → данные должны реально исчезнуть из аналитики
Покажи тест: повторная загрузка → ошибка "уже загружен"
Покажи что normalizeRegionName используется в fileProcessor, percapita и employee matching